import Car from './components/car';
import Plane from './components/plane';

// Car object, created by Car Class constructor
const car = new Car();

// plane object, created by Plane Class constructor
const plane = new Plane();

// car object
console.log(car);

// plane object
console.log(plane);